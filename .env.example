# Copy this file to .env and adjust as needed.
# Shared by docker-compose, the C++ app, and the Alembic migrations,
# so all three always point at the same database.

XDOKU_DB_USER=xdoku_owner
XDOKU_DB_PASSWORD=xdoku_local_password
XDOKU_DB_NAME=xdoku
XDOKU_DB_HOST=localhost
XDOKU_DB_PORT=5432
